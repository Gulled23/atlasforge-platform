---
- name: Update apt package cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install common operational packages
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - unzip
      - git
      - jq
      - vim
      - htop
    state: present
